{
  "scripts": {
    "build": "rimraf ./dist && tsc",
    "test": "yarn build && yarn run promises-aplus-tests ./index.test.js"
  },
  "devDependencies": {
    "promises-aplus-tests": "^2.1.2",
    "rimraf": "^3.0.2",
    "typescript": "^4.1.3"
  }
}
